name: ppo-and-friends

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"

    - name: Install dependencies
      run: |
        sudo apt install libopenmpi-dev
        python3 -m pip install --upgrade pip
        python3 -m pip install setuptools==65.5.0
        python3 -m pip install pytest
        python3 -m pip install .

    - name: Test with pytest
      run: |
        python3 -m pip install .[gym]
        cd test
        pytest tests/train/test_gym.py

        cd ../
        python3 -m pip install .[gymnasium]
        cd test
        pytest tests/train/ -k "gymnasium or zoo"

        cd ../
        python3 -m pip install .[abmarl]
        cd test
        pytest tests/train/test_abmarl.py
